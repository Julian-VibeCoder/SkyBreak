# Bugix Design
- Remove orphaned init_db_fixed.py, empty db files, tmpbkhkvsou.db
- Deduplicate ci.yml/vacation.yml (keep ci.yml), harden docker-publish.yml permissions, disable PR push
- Fix Dockerfile port consistency + add .dockerignore
- Update docker-compose to match EXPOSE 80


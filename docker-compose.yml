version: "3.9"
services:
  app:
    build: .
    volumes: ["/opt/skybreak/:/data/"]
    ports: ["80:80"]
    command: python -m skybreak.app
  frontend:
    image: nginx:alpine
    volumes: ["./frontend:/usr/share/nginx/html:ro"]
    ports: ["8080:80"]
